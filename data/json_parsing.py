import json
import numpy

# json container
# tuple (a, b): a, type; b, items

class ImageFromJson:
    def __init__(self, json_container, dim_x, dim_y, ):
        self.json_container = json.load(json_container)
        self.data_object = data_object
        self.


    def reading_cell(self, cell):
        if cell["type"] == "min":

        elif cell["type"] == "sum":

